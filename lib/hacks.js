// Tries To Undo Needless Capitalization On A Tagline, Which IMDB Users
// Seem To Love. Basically we lowercase all dictionary words in the tagline,
// with the exception of the title itself (which sometimes occurs in taglines,
// eg. Fear City
module.exports.fixCasing = function(tagline, title) {

}